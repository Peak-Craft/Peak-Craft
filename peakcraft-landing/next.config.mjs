/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
        unoptimized: true,
    },
    basePath:'/Peak-Craft',
    assetPrefix: '/Peak-Craft',
};

export default nextConfig;
